Add a card to .shipward/tracker.json from this description: $ARGUMENTS

Rules: status "backlog"; infer type (feature/bug/chore), pri (P1–P3), effort (S/M/L) from the description; next id for the active project's prefix; created = now (ISO); put remaining context in "note". Append a feed entry (by: "claude"). Reply with the card id and a one-line summary only.