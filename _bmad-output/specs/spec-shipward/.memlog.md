---
topic: Shipward — solo-dev kanban progress tracker whose tracker.json is Claude Code's persistent memory
updated: 2026-07-25T15:16
---

- (note) Input: design_handoff_shipward/ (README.md hi-fi spec, CLAUDE.md protocol, 5 slash commands, schema.json, tracker.seed.json, design-reference prototype + modernist-scoped.css). Input is rich and pre-sorted; extracting directly, no elicitation.
- (direction) Alberto asked to run bmad-spec on the handoff after files were placed into shipward_app and git init'd.
- (note) Input: design_handoff_shipward/ (README.md hi-fi spec, CLAUDE.md protocol, 5 slash commands, schema.json, tracker.seed.json, design-reference prototype + modernist-scoped.css). Rich and pre-sorted; extracting directly, no elicitation.
- (direction) Alberto asked to run bmad-spec on the handoff after the files were placed into shipward_app and git init'd.
- (decision) Slug = 'shipward'; folder _bmad-output/specs/spec-shipward. Input describes the product, not an upstream slugged artifact.
- (decision) README.md adopted as companion rather than absorbed: it is the normative hi-fi source for look/behaviour and points at modernist-scoped.css for tokens. Transcribing screen specs and the token table into .md would create two copies and guarantee drift.
- (decision) CLAUDE.md adopted as companion: load-bearing memory protocol, owned by the handoff, already placed at repo root, bmad-spec does not edit it.
- (decision) Two spec-authored companions only: data-contract.md (schema + derived views + id generation) and interaction-rules.md (state machine + verbatim feed copy + relative-time thresholds). Consolidating content scattered across README sections is the value added; the state diagram must live in a companion per Spec Law.
- (note) Token preservation check: modernist-scoped.css carries a --color-accent-2 ramp (100-900, base #ef6853) that README 'Design Tokens' omits. CSS is normative so the ramp survives via the adopted README+CSS path; recorded so the omission is on the record.
- (note) Environment finding: bmad-help.csv lists 18 bmad-wds-* skills that are not installed; WDS is installed as wds-N-* instead. Catalogue routing for WDS is unreliable; bmad-wds-design-delivery (marked required before WDS build) does not exist on disk.
- (capability) CAP-1 board at a glance; CAP-2 drag to change status; CAP-3 create/edit/delete cards; CAP-4 archive; CAP-5 raw data + copy; CAP-6 Claude activity strip; CAP-7 live reflect of on-disk edits; CAP-8 atomic non-corrupting writes; CAP-9 project switcher; CAP-10 Claude runs off the tracker not the conversation.
- (assumption) Polling GET ~3s chosen over fs.watch+SSE; project seeded shipward/SW; single project at launch; no optimistic concurrency on PUT (last write wins per README); UI leaves commit null on push.
- (question) Four open questions recorded: MCP CONNECTED tag has no MCP behind it; card delete contradicts CLAUDE.md 'never delete'; shipped-this-month precedence between pushed and shipped; whether projects[] is UI-editable.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6 + 8): PASS. All 10 capabilities carry intent+success; every constraint rules something out; 7 non-goals; success signal is a runnable demonstration; CAP ids unique and first-derive.
- (event) Self-validate pass 2 (preservation): PASS. Every README section routed - Overview to Why, Fidelity/Architecture/Assets to Constraints and Non-goals, Data schema and State Management to data-contract.md, Interactions to interaction-rules.md, Screens and Design Tokens to the adopted README+CSS, memory protocol to CAP-10 and adopted CLAUDE.md. Wrapper-only content dropped on the record: README 'Files in this bundle' manifest, prototype-viewing instructions, and support.js (source explicitly marks it ignore/plumbing).
- (event) Spec derived and validated: SPEC.md + data-contract.md + interaction-rules.md, 2 adopted companions, 2 absorbed sources. All companion and source paths verified to resolve from the spec folder.
