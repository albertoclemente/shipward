Start work on card $ARGUMENTS in .shipward/tracker.json:
1. Set status "claude", claude "working"; create + set "branch" (feat/|fix/|chore/ from type, kebab, ≤3 words) and check it out.
2. Append a feed entry.
3. Read the card's "note" for context, then begin the task itself.
If the id doesn't exist, list the closest backlog cards and stop.